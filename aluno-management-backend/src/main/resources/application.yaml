spring:
  application:
    name: aluno-management
  datasource:
    url: jdbc:postgresql://localhost:5432/alunos_db
    username: postgres
    password: lala

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
